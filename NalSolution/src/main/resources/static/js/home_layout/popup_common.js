/**
 * Set tittle for popup
 */
function setMessagePopup(tittle, content, nameButton){
	$("#title-popup").html(tittle);
	$("#content-popup").html(content);
	$("#btn-success-popup").html(nameButton);
}